resource "vsphere_resource_pool" "XXX" {
  name                     = "XXX"
  parent_resource_pool_id  = "${data.vsphere_resource_pool.resource_pool.id}"
  cpu_limit                = -1 # Unit: MHz
  cpu_expandable           = true
  cpu_share_level          = "normal"
  memory_limit             = -1
  memory_share_level       = "normal"
  scale_descendants_shares = "disabled"
}

