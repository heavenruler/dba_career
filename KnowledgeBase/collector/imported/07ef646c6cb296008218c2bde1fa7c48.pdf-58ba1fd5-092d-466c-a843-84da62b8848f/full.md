视化 234
博客 下载 学习 社区 GitCode InsCodeAI 会议 搜索 AI 搜索 登录 会员中心 消息 历史 创作中心 创作
基于AI量化模型解析美国7月CPI数据：核
心通胀因子与降息预期的动态关联
MySQL查询为什么选择使用这个索引？——基于MySQL 8.0.22索引成本计算 带你了解关于蛋白质翻译后修饰的简单介绍
砖业洋__ 阅读量1.9w 收藏 55 点赞数 39 于 2021-09-28 17:31:33 发布 CC 4.0 BY-SA版权
最新文章 分类专栏： 文章标签： MySQL MySQL索引成本 MySQL索引优化 MySQL索引选择的原因 MySQL索引的判断依据
MySQL如何确定使用哪个索引 Spring高手之路26——全方位掌握事务监听
器
Byzer 白泽 文章已被社区收录 加入社区 Spring高手之路25——深入解析事务管理的
切面本质
Spring高手之路24——事务类型及传播行为 MySQL 专栏收录该内容 17 篇文章 订阅专栏
实战指南
2024年 10篇 2023年 20篇
本文深入解析了MySQL查询优化的过程，包括单表查询和连接查询的成本计算。通过实例分析，展示了如何根据成本常 2022年 11篇 2021年 29篇
数来评估全表扫描、索引扫描等不同执行计划的成本，并讨论了连接查询中不同表连接顺序的影响。内容涵盖了成本计算的各个组成 2020年 50篇 2019年 17篇
部分，如I/O成本、CPU成本，以及如何根据搜索条件选择最优索引。此外，还提到了外连接与内连接在特定情况下的等价性。 2018年 227篇
摘要生成于 C知道 ，由 DeepSeek-R1 满血版支持， 前往体验 >
目录
本文参考《 MySQL 是怎样运行的》，与书中不同的是，书中数据库版本 ，本文使用 ，不同版本数据库计算成本常 5.7.22 8.0.22 文章目录 数是不同的，书中是 条记录，我这里是近 条记录，经过实践，是对于书中的补充和验证，计算的成本和实际成本对比，让 1W 10W
大家更容易理解 为什么要使用这个索引。 MySQL 1.什么是成本
1.1 如何查看MySQL的成本常数
2.单表查询的成本
文章目录
2.1 基于成本的优化步骤 1.什么是成本
1.1 如何查看MySQL的成本常数 3.连接查询的成本(作为开发人员，必看)
2.单表查询的成本 3.1 连接查询的准备
2.1 基于成本的优化步骤
3.2 条件过滤 (Condition filtering) 3.连接查询的成本(作为开发人员，必看)
3.1 连接查询的准备 3.3 两表连接的成本分析（这部分对开发人员写sql很重要）
3.2 条件过滤 (Condition filtering) 3.4 多表连接的成本分析（选读，了解）
3.3 两表连接的成本分析（这部分对开发人员写sql很重要） 觉得还不错? 一键收藏
3.5 外连接和内连接等同的情况（外连接可转为内连接） 3.4 多表连接的成本分析（选读，了解）
砖业洋__ 39 55 2 分享 打赏 关注 专栏目录 3.5 外连接和内连接等同的情况（外连接可转为内连接） 收起
1.什么是成本
我们知道， 查询会选择成本最低，或代价最低的那种方式去真正的执行查询。 MySQL
的查询成本分为下面两个部分 MySQL
成本 I/O
我们的表经常使用的 、 存储引擎都是将数据和索引都存储到磁盘上的，当查询表中的记录时，需要先把数据或者索 MyISAM InnoDB
引加载到内存中，然后再进行操作。 这个从磁盘到内存的加载过程损耗的时间称为 成本。 I/O
成本 CPU
读取以及检测记录是否满足对应的搜索条件、对结果集进行排序等这些操作损耗的时间称之为 成本。 CPU
对于 存储引擎来说，页是磁盘和内存之间交互的基本单位， 规定：读取一个页面花费的成本默认是 ，读取以及检 InnoDB MySQL 1.0
测一条记录是否符合搜索条件的成本默认是 ( 版本是 )。 、 这些数字称之为成本常数。 0.1 MySQL 5.7.22 0.2 1.0 0.1
在读取记录时，即使不需要检测记录是否符合 条件，其成本都算是 ，即访问一条记录所需的成本常数就是 ，不管有 where 0.1 0.1
无 条件。 where
1.1 如何查看MySQL的成本常数
AI写代码 sql
1 SELECT * FROM mysql . server_cost ;
从 中的内容可以看出来，目前在 层的一些操作对应的成本常数有以下几种： server_cost server

默认值 （括号中是 成本常数名称 描述 MySQL5.7.22测出的值）
创建基于磁盘的临时表的成本，如果增大这个值的话会让优化器尽量少的创建基于磁 disk_temptable_create_cost 20.0 （40.0） 盘的临时表。
向基于磁盘的临时表写入或读取一条记录的成本，如果增大这个值的话会让优化器尽 disk_temptable_row_cost 0.5 （1.0） 量少的创建基于磁盘的临时表。
两条记录做比较操作的成本，多用在排序操作上，如果增大这个值的话会提升filesort key_compare_cost 0.05 （0.1） 的成本，让优化器可能更倾向于使用索引完成排序而不是filesort。
创建基于内存的临时表的成本，如果增大这个值的话会让优化器尽量少的创建基于内 memory_temptable_create_cost 1.0 （2.0） 存的临时表。
向基于内存的临时表写入或读取一条记录的成本，如果增大这个值的话会让优化器尽 memory_temptable_row_cost 0.1 （0.2） 量少的创建基于内存的临时表。
这个就是我们上面说的检测一条记录是否符合搜索条件的成本，增大这个值可能让优 row_evaluate_cost 0.1 （0.2） 化器更倾向于使用索引而不是直接全表扫描。
AI写代码 sql
1 SELECT * FROM mysql . engine_cost ;
默认值 （括号中是
成本常数名称 MySQL5.7.22测出的 描述
值）
从磁盘上读取一个块对应的成本。 请注意我使用的是块，而不是页这个词儿。 对于InnoDB存储引
io_block_read_cost 1.0 （1.0） 擎来说，一个页就是一个块 ，不过对于MyISAM存储引擎来说，默认是以4096字节作为一个块的。
增大这个值会加重I/O成本，可能让优化器更倾向于选择使用索引执行查询而不是执行全表扫描。
memory_block_read_cost 0.25 （1.0） 与上一个参数类似，只不过衡量的是 从内存中读取一个块对应的成本 。
版本怎么从内存中和从磁盘上读取一个块的默认成本都是 呢？这主要是因为在 的实现中，并不能准确预 5.7.22 1.0 MySQL 5.7.22
测某个查询需要访问的块中有哪些块已经加载到内存中，有哪些块还停留在磁盘上。所以 很粗暴的认为不管这个块有没有加载到 MySQL
内存中，使用的成本都是 。 1.0
至于为什么在 版本中成本常数变小了呢？那是因为数据库的算法在不断的优化，能更加准确的预测块是否加载到内存中了。所 8.0+
以在不同数据库版本查看 执行计划，选择的实际索引可能有所不同。 sql
我们这里查询的是 库里面的 和 表，在大公司中，一般人根本没权限查看这个 库的内 mysql server_cost engine_cost mysql
容。因为 库相当重要，它存储了 的用户账户和权限信息、一些存储过程和事件的定义信息、一些运行过程中产生的日 mysql MySQL
志信息、一些帮助信息以及时区信息等等。所以大家可以在自己电脑自行验证，公司电脑可能限制了权限。
2.单表查询的成本
和前面几篇文章一样，首先列出建表语句，后面例子均在此表基础上举例说明。
AI写代码 sql
1 CREATE TABLE demo_info (
2 id INT NOT NULL auto_increment ,
3 key1 VARCHAR ( 100 ) ,
4 key2 INT ,
5 key3 VARCHAR ( 100 ) ,
6 key_part1 VARCHAR ( 100 ) ,
7 key_part2 VARCHAR ( 100 ) ,
8 key_part3 VARCHAR ( 100 ) ,
9 common_field VARCHAR ( 100 ) ,
10 PRIMARY KEY ( id ) ,
11 KEY idx_key1 ( key1 ) ,
2.1 基于成本的优化步骤
在真正执行一条单表查询语句之前， 的优化器会找出执行该语句所有可能使用的方案，对比之后找出成本最低的方案，这个 MySQL
成本最低的方案就是所谓的 执行计划 ，之后才会调用存储引擎提供的接口真正的执行查询。过程如下：
1.根据搜索条件，找出所有可能使用的索引
2.计算全表扫描的代价
3.计算使用不同索引执行查询的代价（先分析唯一索引、再普通索引）
4.对比各种执行方案的代价，找出成本最低的那一个
接下来，我们来看一下这 个步骤的细节分析 4
用一个 来具体分析下 sql

AI写代码 sql
1 explain
2 select * from demo_info where
3 key1 in ( 'a' , 'b' , 'c' ) and
4 key2 > 10 and key2 < 1000 and
5 key3 > key2 and
6 key_part1 like '%hello%' and
7 common_field = '123' ;
可以看到这里实际使用的索引是 ，这个结果优化器是怎么计算出来的呢？来，一起分析！ uk_key2
1.根据搜索条件，找出所有可能使用的索引
，这个搜索条件可以使用普通索引 。 key1 IN ('a', 'b', 'c') idx_key1
，这个搜索条件可以使用唯一索引 。 key2 > 10 AND key2 < 1000 uk_key2
，这个搜索条件的索引列由于没有和常数比较，因此不能产生合适的扫描区间。 key3 > key2
， 通过 操作符和以通配符开头的字符串做比较，不可以适用索引。 key_part1 LIKE '%hello%' key_part1 LIKE
，由于该列上压根儿没有索引，所以不会用到索引。 common_field = '123'
综上所述，上边的查询语句可能用到的索引（也就是 ）有 和 。 possible keys idx_key1 uk_key2
2.计算全表扫描的代价
对于 存储引擎来说， 全表扫描的意思就是把聚集索引中的记录都依次与给定的搜索条件进行比较，把符合搜索条件的记录加 InnoDB
入到结果集中。 所以需要将聚集引 对应的页面 加载到内存中，然后再检测记录是否符合搜索条件。由于查询成本 = 成本 + 成 I/O CPU
本，所以计算全表扫描的代价需要两个信息：
聚集索引占用的页面数
该表中的记录数
那这两个信息从哪里来呢？
可以用 语句来查看表的统计信息，如果要看指定的某个表的统计信息，在该语句后加对应的 语句就好了， show table status like
比方说我们要查看 这个表的统计信息可以这么写： demo_info
AI写代码 sql
1 show table status like 'demo_info'
切换到表单视图方便查看
表中实际有 条记录，与预估的是不同的。 99896
虽然出现了很多统计项，但是我们只需要关注两个选项。
Rows
本选项表示表中的记录条数。对于使用 存储引擎的表来说，该值是准确的，对于使用 存储引擎的表来说，该值是一 MyISAM InnoDB
个估计值。从查询结果我们也可以看出来，由于我们的 表是使用 存储引擎的，尽管表实际有 条记录，但是 demo_info InnoDB 99896
显示的 值有 条记录， 这个值比实际记录条数偏大偏小都有可能，总之对于 是不准确的。 show table status Rows 100548 InnoDB

Data_length
表示表占用的存储空间字节数。使用 存储引擎的表来说，该值就是数据文件的大小， 对于使用 存储引擎的表来说， MyISAM InnoDB
该值就相当于聚集索引占用的存储空间大小 ，也就是说可以这样计算该值的大小：
= 聚集索引的页面数量 x 每个页面的大小 Data_length
我们的 使用默认 的页面大小，而上边查询结果显示 的值是 ，所以可以反向来推导出聚集索 demo_info 16KB Data_length 5783552
引的页面数量：
聚簇索引的页面数量 = 5783552 ÷ 16 ÷ 1024 = 353
现在已经得到了聚集索引占用的页面数量以及该表记录数的估计值，接下来就可以计算全表扫描成本了，但是 代码中有一些 MySQL
硬编码进行成本的微调，微调值没有注释，含义无从得知，但是不影响后续分析。
看一下全表扫描成本的计算过程
成本 I/O
353 x 1.0 + 1.1 = 354.1
指的是聚集索引占用的页面数， 指的是加载一个页面的成本常数，后边的 是一个微调值，我们不用在意。 353 1.0 1.1
成本： CPU
100548 x 0.1 + 1.0 = 10055.8
指的是统计数据中表的记录数(看 列)，对于 存储引擎来说是一个估计值， 指的是访问一条记录所需的成本常 100548 Rows InnoDB 0.1
数，后边的 是一个微调值，我们不用在意。 1.0
总成本：
354.1 + 10055.8 = 10409.9
综上所述，对于 的全表扫描所需的总成本就是 。 demo_info 10409.9
完整的用户记录其实都存储在聚集索引对应 树的叶子节点中，所以只要我们通过根节点获得了最左边的叶子节点，就可以沿 B+
着叶子节点组成的双向链表把所有记录都查看一遍。也就是说全表扫描这个过程其实有的 树非叶子节点是不需要访问的（即有的 B+
目录页是不需要访问的），但是 在计算全表扫描成本时直接使用聚集索引占用的页面数（包含所有目录页）作为计算 成 MySQL I/O
本的依据，是不区非叶子节点和叶子节点的，有点简单粗暴，大家注意一下。
3.计算使用不同索引执行查询的代价
从第 步分析我们得到，上述查询可能使用到 和 这两个索引，我们需要分别分析单独使用这些索引执行查询的成 1 idx_key1 uk_key2
本，最后还要分析是否可能使用到索引合并。 查询优化器先分析使用唯一二级索引的成本，再分析使用普通索引的成本 ，所以我 MySQL
们也先分析 的成本，然后再看使用 的成本。 uk_key2 idx_key1
(1) 使用 执行查询的成本分析 uk_key2
对应的搜索条件是： ，也就是说对应的范围区间就是： ，使用 执行查 uk_key2 key2 > 10 AND key2 < 1000 (10, 1000) uk_key2
询的步骤如下
1.先从 索引中定位 在 这个区间内的记录，找到记录对应的 列的值 uk_key2 key2 (10, 1000) id
2.再回表到聚集索引中根据上一步得到的 值，找到完整的用户记录 id
对于使用非聚集索引 + 回表方式的查询， 查询的成本依赖两个方面的数据：扫描区间的数量和需要回表的记录数 MySQL
扫描区间的数量
查询优化器粗暴的认为 读取索引的一个扫描区间的 成本和读取一个页面的 成本是相同的 。本例中使用 的扫描区间 I/O I/O uk_key2
只有一个： ， 无论该扫描区间的非聚集索引有多少条(可能是很大的区间几十万条)、到底占用了多少页面，都只认为和读取 (10, 1000)
一个页面的 成本一样（有多少个扫描区间，相当于和读对应数量的页的 成本一样） ，所以相当于访问这个扫描区间的非聚集索引 I/O I/O
付出的 成本就是： I/O
1 x 1.0 = 1.0 （有几个扫描区间，就相当于要计算读几个页面的成本，这点容易忽视）
其中， 是一个扫描区间， 是表示读取一个页面的 成本 1 1.0 I/O
需要回表的记录数
查询优化器需要计算非聚集索引的某个扫描区间到底包含多少条记录，对于本例来说就是要计算 在 这个扫描区 uk_key2 (10, 1000)
间中包含多少非聚集索引记录，计算过程是这样的：
步骤1：先根据 这个条件访问一下 对应的 树索引，找到满足 这个条件的第一条记录（我们把这条 key2 > 10 uk_key2 B+ key2 > 10
记录称之为区间最左记录）。在 数树中定位一条记录是非常快的，是常数级别的，这个过程的性能消耗是可以忽略不计。 B+
步骤2：然后再根据 这个条件继续从 对应的 树索引中找出最后一条满足这个条件的记录（我们把这条记录 key2 < 1000 uk_key2 B+
称之为区间最右记录），这个过程的性能消耗同样可以忽略不计的。
步骤3：如果区间最左记录和区间最右记录相隔不太远（在 这个版本里，只要相隔不大于 个页面即可），就可以精 MySQL 5.7.22 10
确统计出满足 条件的二级索引记录条数。否则需要沿着区间最左记录向右读 个页面，计算每个页面平 key2 > 10 AND key2 < 1000 10
均包含多少记录，然后用这个平均值乘以区间最左记录和区间最右记录之间的页面数量就可以了。

数据页结构中有一个 的部分， 中有一个 的属性，表示该页面中目前有多少条记录。 Page Header Page Header PAGE_N_RECS
所以，在区间不太远的情况下，我们可以把每个页面中的 属性值加起来就好了，而不用依次遍历每条记录再统计。 PAGE_N_RECS
那么问题又来了，怎么估计区间最左记录和区间最右记录之间有多少个页面呢？解决这个问题还得回到 树索引的结构中来： B+
如图，我们假设区间最左记录在 中，区间最右记录在 中，那么计算区间最左记录和区间最右记录之间的页面数量就是计算 页b 页c 页
和 之间有多少页面，此时只需要计算 和 的父级目录页之间隔着几条记录即可，在一个页面中统计两条记录之间有几条记录 b 页c 页b 页c
的成本就相当低了。
如果 和 之间的页面实在太多，以至于 和 对应的目录项记录都不在一个页面中，则继续递归到父级目录的父级目录， 页b 页c 页b 页c
再统计所在页面之间有多少个页面。一个 树有 层高已经很了不得了，所以这个统计过程也不是很耗费性能。 B+ 4
知道了如何统计非聚集索引某个扫描区间的记录数之后，让我们回到具体问题中来，就是要找到需要回表的记录数，根据上述算法测
得 在区间 之间有 条记录（我的表中查出来就是 条记录）。读取这 条二级索引记录需要付出的 成本 uk_key2 (10, 1000) 883 883 883 CPU
就是：
883 x 0.1 + 0.01 = 88.31
其中 是需要读取的非聚集索引记录条数， 是读取一条记录成本常数， 是微调。 883 0.1 0.01
在通过非聚集索引获取到记录之后，还有 个步骤： 2
1.回表，根据这些记录里的主键值到聚集索引中执行回表操作
评估回表操作的 成本依旧很粗略， 认为每次回表操作都相当于访问一个页面，也就是说非聚集索引扫描区间中有 MySQL I/O MySQL
多少记录，就需要进行多少次回表操作，也就是需要进行多少次页面 。前面在使用 唯一索引执行查询时，有 条非聚集 I/O uk_key2 883
索引记录需要进行回表操作，所以回表操作带来的 成本 就是： I/O
883 x 1.0 = 883.0
其中 是预计的非聚集索引记录数， 是一个页面的 成本常数。 883 1.0 I/O
2.回表操作后得到的完整用户记录，然后再检测其他搜索条件是否成立
回表操作的本质就是通过二级索引记录的主键值到聚集索引中找到完整的用户记录，然后再检测除 这 key2 > 10 AND key2 < 1000
个搜索条件以外的搜索条件是否成立。因为我们通过扫描区间获取到非聚集索引记录共 条，也就对应着聚集索引中 条完整的用 883 883
户记录，读取并检测这些完整的用户记录是否符合其余的搜索条件的 成本为 CPU
883 * 0.1 = 88.3
其中 是待检测的记录的条数， 是检测一条记录是否符合给定搜索条件的成本常数。 883 0.1
所以本例使用 执行查询成本如下： uk_key2
成本: 1.0 + 883 * 1.0 = 884.0 (1个扫描区间的成本 + 非聚集索引回表的成本) I/O
成本: 883 * 0.1 + 0.01 + 883 * 0.1 = 176.61 （根据搜索条件读取非聚集索引的成本 + 回表后根据搜索条件读取聚集索引的成本） CPU
综上，使用 执行查询的总成本就是 uk_key2 884.0 + 176.61 = 1060.61
注意：方便记忆法：若走索引，读非聚集索引条件有微调，读聚集索引没有，扫描区间、回表任何一条记录都相当于读取一页。若
不走索引，微调值另外分析，和前者不同。
怎么验证？
直接上代码，不用废话，来个华丽的验证

AI写代码 sql
1 explain format = json
2 select * from demo_info where
3 key1 in ( 'a' , 'b' , 'c' ) and
4 key2 > 10 and key2 < 1000 and
5 key3 > key2 and
6 key_part1 like '%hello%' and
7 common_field = '123' ;
运行结果
AI写代码 sql
1 {
2 "query_block" : {
3 "select_id" : 1 ,
4 "cost_info" : {
5 "query_cost" : "1060.61" = = = = = = = = = = = = = = = = = = = = = = = = 看见没？看我，我就是成本，好好看，仔细看
6 } ,
7 "table" : {
8 "table_name" : "demo_info" ,
9 "access_type" : "range" ,
10 "possible_keys" : [
11 "uk_key2" ,
截个图代表结果不是我手动改的，计算和验证结果一样！！
（2）使用 执行查询的成本分析 idx_key1
对应的搜索条件是： ，也就是说相当于 个单点区间： idx_key1 key1 IN ('a', 'b', 'c') 3 ['a', 'a']，['b', 'b']，['c', 'c']
与使用 的情况类似，我们也需要计算使用 时需要访问的范围区间数量以及需要回表的记录数： uk_key2 idx_key1
扫描区间数量
使用 执行查询时很显然有 个单点扫描区间，所以访问这 个扫描间的非聚集索引付出的 成本就是： idx_key1 3 3 I/O
3 x 1.0 = 3.0
需要回表的记录数
每个单点区间都需要查找一遍对应的非聚集索引记录数：
查找单点区间 对应的非聚集索引记录数 ['a', 'a']
计算单点区间对应的非聚集索引记录数和计算连续范围区间对应的非聚集索引记录数是一样的，都是先计算区间最左记录和区
间最右记录，然后再计算它们之间的记录数，具体算法上边已经说过。最后计算得到单点区间 对应的非聚集索引记录数 ['a','a']
是： 。 99861
查找单点区间 对应的二级索引记录数 ['b', 'b']
与上同理，计算得到本单点区间对应的记录数是： 。 0
查找单点区间 对应的二级索引记录数 ['c', 'c']
与上同理，计算得到本单点区间对应的记录数是： 。 1
由于我造数据有点特殊，几乎所有记录都在 扫描区间内，但是不影响分析。 ['a', 'a']
所以，这三个单点区间总共需要回表的记录数就是： 99861 + 0 + 1 = 99862 ，读取这些非聚集索引记录的 成本就是： 99862 * 0.1 + CPU
0.01 = 9986.21
得到总共需要回表的记录数之后，还要继续考虑：
根据这些记录中的主键值到聚集索引中执行回表操作，所需的 成本就是： I/O

99862 x 1.0 = 99862.0 （每次回表相当于访问一个页面）
针对回表操作后得到的完整用户记录，比较 其他 搜索条件是否成立。这一步骤对应的 成本 CPU
99862 * 0.1 = 9986.2
所以本例中使用 执行查询的成本就如下所示： idx_key1
成本： 3.0 + 99862 x 1.0 = 99865.0 (范围区间的数量 + 预估的二级索引记录条数) I/O
成本： 99862 x 0.1 + 0.01 + 99862 x 0.1 = 19972.41 （读取二级索引记录的成本 + 读取并检测回表后聚簇索引记录的成本） CPU
综上所述，使用 执行查询的总成本就是： idx_key1
99865.0 + 19972.41 = 119837.41
这成本远大于全表扫描，所以如果只有 索引，那么优化器一定会选择全表扫描。 idx_key1
（3）是否有可能使用索引合并（ ） Index Merge
本例中有关 和 的搜索条件是使用 连接起来的，而对于 和 都是范围查询，也就是说查找到的非聚 key1 key2 AND idx_key1 uk_key2
集索引记录并不是按照主键值进行排序的，并不满足使用 索引合并的条件，所以并不会使用索引合并。 查询优化 Intersection MySQL
器计算索引合并成本的算法也比较麻烦，这里不讲，理解成本如何计算，知道 会按照这种算法选择索引即可。 MySQL
4. 对比各种执行方案的代价，找出成本最低的那一个
全表扫描成本：10409.9
使用 的成本：1060.61 uk_key2
使用 的成本：119837.41 idx_key1
综上，优化器会选择 索引执行查询 uk_key2
在这个例子中，不管采用 和 执行查询，它们对应的都是 方法。在使用 访问方法执行查询的时候， idx_key1 uk_key2 range range
扫描区间中包含多少条记录，优化器就认为需要进行多少次回表操作，也就相当于需要进行多少次页面 . I/O
如果是 方法， 就给因为回表操作带来的 成本设置了天花板，也就是 访问方法因回表操作带来的 成本最多不 ref MySQL I/O ref I/O
能超过访问 全表记录数 的页面 成本或者 全表扫描 的 成本的 倍。 ÷ 10 I/O I/O 3
实际中，我们想分析 为什么选择这个索引，直接如下例子，强制索引后分析成本，根本不用自己手动计算，本文是给大家分析， MySQL
让大家理解思路。
AI写代码 sql
1 explain format = json
2 select * from demo_info
3 - 强制索引分析成本
4 force index ( idx_key1 )
5 where
6 key1 in ( 'a' , 'b' , 'c' ) and
7 key2 > 10 and key2 < 1000 and
8 key3 > key2 and
9 key_part1 like '%hello%' and
10 common_field = '123' ;
3.连接查询的成本(作为开发人员，必看)
3.1 连接查询的准备
我们直接构造一个和 表一模一样的 表。为了简便起见，我们把 表称为 表，把 表 demo_info demo_info2 demo_info d1 demo_info2
称为 表。 d2
3.2 条件过滤 (Condition filtering)
我们上一篇说过， 中连接查询采用的是嵌套循环连接算法，驱动表只访问一次，被驱动表可能会被访问多次，所以对于两表 MySQL
连接查询来说，它的查询成本由下边两个部分构成：
单次查询驱动表的成本
多次查询被驱动表的成本（每查询到一条驱动表记录，就会去查询被驱动表，所以具体查询多少次取决于驱动表查询的结果集中有多
少条记录）
我们把对驱动表进行查询后得到的记录条数称之为驱动表的扇出（ ） 。显然，驱动表的扇出值越小，对被驱动表的查询次数 fanout
也就越少，连接查询的总成本也就越低。当查询优化器想计算整个连接查询所使用的成本时，就需要计算出驱动表的扇出值，有的时候扇
出值的计算是很容易的，比如下面这两个查询：
查询一：
AI写代码 sql
1 SELECT * FROM demo_info AS d1 INNER JOIN demo_info2 AS d2 ;
假设使用 表作为驱动表，很显然就只能使用全表扫描的方式对驱动表执行单表查询，驱动表中有多少记录，扇出值就是多少。我 d1
们前边说过， 统计数据中 表的记录行数是 ，也就是说优化器就直接会把 当 show table status like 'demo_info' d1 100548 100548
作在 表的扇出值。没有连接条件的表连接查询会产生笛卡尔积，一般都会写条件。 d1

为什么我们分析内连接老是假设驱动表？难道左表不是驱动表？不一定，内连接左右表顺序可以任意互换，优化器会优化其连
接顺序的。后面会分析，优化器会计算成本后再确定哪个做为驱动表。
查询二：
AI写代码 sql
1 SELECT * FROM demo_info AS d1 INNER JOIN demo_info2 AS d2
2 WHERE d1 . key2 > 10 AND d1 . key2 < 1000 ;
仍然假设 表是驱动表的话，很显然对驱动表的单表查询可以使用 索引执行查询。此时 的扫描区间 d1 uk_key2 uk_key2 (10, 1000)
中有多少条记录，那么扇出值就是多少。我们前边计算过，满足 的扫描区间 的记录数是 条，也就是说本查询中 uk_key2 (10, 1000) 883
优化器会把 当作驱动表 的扇出值。 883 d1
有的时候扇出值的计算非常困难，比如这两种情况下计算驱动表扇出值时需要靠 ‘猜’：
如果使用全表扫描的方式执行单表查询，那么计算驱动表扇出值的时候需要猜测满足全部搜索条件的记录有多少条。
如果使用的是索引执行的单表扫描，那么计算驱动表扇出的时候需要猜测除了满足形成索引扫描区间的搜索条件外，还满足其
他搜索条件的记录有多少条。
例子如下：
查询三：
AI写代码 sql
1 SELECT * FROM demo_info AS d1 INNER JOIN demo_info2 AS d2
2 WHERE d1 . common_field > 'xyz' ;
本查询和查询一类似，只不过对于驱动表 多了一个 的搜索条件。查询优化器又不会真正的去执行查询， d1 common_field > 'xyz'
所以它只能猜这 记录里有多少条记录满足 条件。 100548 common_field > 'xyz'
查询四：
AI写代码 sql
1 SELECT * FROM demo_info AS d1 INNER JOIN demo_info2 AS d2
2 WHERE d1 . key2 > 10 AND d1 . key2 < 1000 AND
3 d1 . common_field > 'xyz' ;
本查询和查询二类似，只不过在查询驱动表 也多了一个 的搜索条件。不过因为本查询可以使用 d1 common_field > 'xyz' uk_key2
索引，所以只需要从符合二级索引范围区间的记录中猜有多少条记录符合 条件，也就是只需要在 条记录中猜 common_field > 'xyz' 883
测有多少条记录符合 条件。 common_field > 'xyz'
查询五：
AI写代码 sql
1 SELECT * FROM demo_info AS d1 INNER JOIN demo_info2 AS d2
2 WHERE d1 . key2 > 10 AND d1 . key2 < 1000 AND
3 d1 . key1 IN ( 'a' , 'b' , 'c' ) AND
4 d1 . common_field > 'xyz' ;
本查询和查询二类似，不过在驱动表 选取 索引执行查询后，优化器需要在非聚集索引扫描区间的记录中猜有多少条记录符合 d1 uk_key2
下边两个条件：
AI写代码 sql
1 key1 IN ( 'a' , 'b' , 'c' )
2
3 common_field > 'xyz'
也就是优化器需要猜在 条记录中有多少符合上述两个条件的。 883
这个猜的过程称之为 。当然，这个过程可能会使用到索引，也可能使用到统计数据，评估过程很复杂， condition filtering
我也不知道。引用书中的话说，这个 的功能，就是还要猜一猜剩余的那些搜索条件能把驱动表中的记录再过 condition filtering
滤多少条，其实本质上就是为了让成本估算更精确。 设计 的大叔们称之为启发式规则（ ），大家有兴趣的可以再 MySQL heuristic
深入了解一下哈。
3.3 两表连接的成本分析（这部分对开发人员写 sql 很重要）
连接查询的成本计算公式是这样的：
连接查询总成本 = 单次访问驱动表的成本 + 驱动表扇出值 x 单次访问被驱动表的成本
对于 左（外）连接和右（外）连接 查询来说，它们的 驱动表是固定 的，左连接驱动表是左表，右连接驱动表是右表。只需要分别为驱动表
和被驱动表选择成本最低的访问方法，就可以得到最优的查询方案。
可是对于 内连接 来说， 驱动表和被驱动表的位置是可以互换 的，所以需要考虑两个方面的问题：
不同的表作为驱动表最终的查询成本可能是不同的，也就是需要考虑最优的表连接顺序。
然后分别为驱动表和被驱动表选择成本最低的访问方法。

很显然， 计算内连接查询成本的方式更麻烦一些 ，下边我们就以内连接为例来看看如何计算出最优的连接查询方案。
左（外）连接和右（外）连接查询在某些特殊情况下可以被优化为内连接查询，我们在后面的文章会提到。
来来来，看例子，用例子说话
AI写代码 sql
1 SELECT * FROM demo_info AS d1 INNER JOIN demo_info2 AS d2
2 ON d1 . key1 = d2 . common_field
3 WHERE d1 . key2 > 10 AND d1 . key2 < 1000 AND
4 d2 . key2 > 1000 AND d2 . key2 < 2000 ;
首先要告诉大家的是，连接、搜索条件写在 和写在 是没有性能上的区别的，优化器会自动优化，之所以分开写，完全是因为这 on where
样可读性好而不是性能原因，请一定记住这句话。
可以选择的内连接顺序有两种：
连接 ，也就是 作为驱动表， 作为被驱动表。 d1 d2 d1 d2
连接 ，也就是 作为驱动表， 作为被驱动表。 d2 d1 d2 d1
查询优化器需要分别考虑这两种情况下的最优查询成本，然后选取那个成本更低的连接顺序以及该连接顺序下各个表的最优访问方法作为
最终的查询计划。我们分别来看一下（定性分析）：
1.使用 作为驱动表 d1
分析对于驱动表的成本最低的执行方案，看一下涉及 表单表的搜索条件有哪些： d1
d1.key2 > 10 AND d1.key2 < 1000
这个查询可能使用到 索引，从全表扫描和使用 这两个方案中选出成本最低的那个，这个过程我们上边说过了，很 uk_key2 uk_key2
显然使用 执行查询的成本更低些。 uk_key2
然后分析对于被驱动表的成本最低的执行方案，此时涉及被驱动表 的连接搜索条件就是： d2
= 常数（这对于 只能全表扫描了） d2.common_field d2
（可以用到 索引） d2.key2 > 1000 AND d2.key2 < 2000 uk_key2
很显然，第一个条件由于 没有用到索引，此时访问 表时可用的方案也是全表扫描和使用 两种，显然使用 common_field d2 uk_key2
的成本更小。 uk_key2
所以此时使用 作为驱动表时的总成本就是（暂时不考虑使用 对成本的影响）： d1 join buffer
使用 访问 的成本 + 的扇出 × 使用 访问 的成本 uk_key2 d1 d1 uk_key2 d2
2.使用 作为驱动表 d2
分析对于驱动表的成本最低的执行方案，看一下涉及 表单表的搜索条件有哪些： d2
d2.key2 > 1000 AND d2.key2 < 2000
所以这个查询可能使用到 索引，从全表扫描和使用 这两个方案中选出成本最低的那个，显然使用 执行查 uk_key2 uk_key2 uk_key2
询的成本更低些。
然后分析对于被驱动表的成本最低的执行方案，此时涉及被驱动表 的搜索条件就是： d1
= 常数 d1.key1
d1.key2 > 10 AND d1.key2 < 2000
这时就很有趣了，使用 可以进行 方式的访问，使用 可以使用 方式的访问。这是优化器需要从全表扫 idx_key1 ref uk_key2 range
描、使用 、使用 这几个方案里选出一个成本最低的方案。 idx_key1 uk_key2
这里有个问题，因为 的范围区间是确定的： ，怎么计算使用 的成本我们上边已经说过了，可是在没有真 uk_key2 (10, 1000) uk_key2
正执行查询前， = 常数中的常数值我们是不知道的，一般情况下， 的访问方式要比 成本更低，但是上面分析过，使 d1.key1 ref range
用 访问 成本是更低的。 uk_key2 d1
所以此时使用 作为驱动表时的总成本就是： d2
使用 访问 的成本 + 的扇出 × 使用 访问 的成本 uk_key2 d2 d2 uk_key2 d1
最后优化器会比较这两种方式的最优访问成本，选取那个成本更低的连接顺序去真正的执行查询。 从上边的计算过程也可以看出
来，连接查询成本 “占大头” 的其实是 驱动表扇出数 x 单次访问被驱动表的成本 ，所以我们的优化重点其实是下边这两个部分：
尽量减少驱动表的扇出（小表作为驱动表）
对被驱动表的访问成本尽量低 （好的索引很重要）

这一点对于我们实际书写连接查询语句时十分有用，我们需要 尽量在被驱动表的连接列上建立索引 ，这样就可以使用 访问方法来降低 ref
访问被驱动表的成本了。 如果可以，被驱动表的连接列最好是该表的主键或者唯一二级索引列，这样就可以把访问被驱动表的成本降到更
低了。
所谓的大表还是小表，得等到条件过滤之后记录数的多少才能区分大表还是小表，而不是只看表里总记录数量来判断大表小表。
3.4 多表连接的成本分析（选读，了解）
在分析多表连接的成本之前，首先要考虑一下多表连接时可能产生出多少种连接顺序：
对于两表连接，比如 和 连接，只有 、 这两种连接顺序。（其实相当于 种连接顺序） 表A 表B AB BA 2 × 1 = 2
对于三表连接，比如 、 、 进行连接，有 、 、 、 、 、 这 种连接顺序。（其实相当于 表A 表B 表C ABC ACB BAC BCA CAB CBA 6 3 × 2 ×
种连接顺序） 1 = 6
对于四表连接，则会有 种连接顺序。 4 × 3 × 2 × 1 = 24
对于 表连接的话，则有 种连接顺序，就是 的阶乘（ ）种连接顺序。 n n × (n-1) × (n-2) × ··· × 1 n n!
有n个表进行连接，MySQL查询优化器要每一种连接顺序的成本都计算一遍么？不会！MySQL有很多办法减少因计算不同连接顺序
下的查询成本带来的性能损耗。
提前结束某种顺序的成本评估
在计算各种链接顺序的成本之前，会维护一个全局的变量，这个变量表示当前最小的连接查询成本。如果在分析某个连接顺 MySQL
序的成本时，该成本已经超过当前最小的连接查询成本，那就压根儿不对该连接顺序继续往下分析了。比如说 、 、 三个表进行连 A B C
接，已经得到连接顺序 是当前的最小连接成本（假设为 ），在计算连接顺序 时，发现 和 的连接成本就已经大于 ABC 10.0 BCA B C 10.0
时，就不再继续往后分析连接 的成本了。 A
系统变量 optimizer_search_depth
为了防止无穷无尽的分析各种连接顺序的成本， 有一个 系统变量，如果连接表的个数小于该值， MySQL optimizer_search_depth
那么就继续穷举分析每一种连接顺序的成本，否则只对与 值相同数量的表进行穷举分析。很显然，该值越大， optimizer_search_depth
成本分析的越精确，越容易得到好的执行计划，但是消耗的时间也就越长，否则得到的就不是很好的执行计划，但可以省掉了连接成本的
分析时间。
某些规则压根儿就不考虑某些连接顺序
即使是有上边两条规则的限制，但是分析多个表不同连接顺序成本花费的时间还是会很长，所以 有一些所谓的启发式规则 MySQL
（就是根据以往经验指定的一些规则），凡是不满足这些规则的连接顺序就不分析，这样可以极大的减少需要分析的连接顺序的数量，但
是也可能造成错失最优的执行计划。他们提供了一个系统变量 来控制是否使用这些启发式规则。 optimizer_prune_level
3.5 外连接和内连接等同的情况（外连接可转为内连接）
外连接和内连接在某种情况下是等价的，也就是说可以相互转换的，写 或者 效果一样。只要在 [inner] join left | right join
子句的搜索条件中指定被驱动表的列不为 ，最后的结果集就和内连接结果集一样。 where NULL
例子如下
AI写代码 sql
1 SELECT * FROM demo_info AS d1 LEFT JOIN demo_info2 AS d2
2 ON d1 . key1 = d2 . common_field
3 WHERE d2 . key_part2 is not null ;
因为指定了被驱动表 表的 列不允许为 ，所以 和 的左连接和内连接查询是一样的。当然，我们也可以不用 d2 key_part2 NULL d1 d2
显式指定被驱动表的某个列 ，只要隐含就行，比如 is not null
AI写代码 sql
1 SELECT * FROM demo_info AS d1 LEFT JOIN demo_info2 AS d2
2 ON d1 . key1 = d2 . common_field
3 WHERE d2 . key_part2 = 2 ;
在 子句指定了被驱动表 的 列等于 ，那就肯定不为 了，这和下面内连接语句是等价的。 where d2 key_part2 2 null
AI写代码 sql
1 SELECT * FROM demo_info AS d1 JOIN demo_info2 AS d2
2 ON d1 . key1 = d2 . common_field
3 WHERE d2 . key_part2 = 2 ;
我们把这种在外连接查询中，指定 子句中包含被驱动表中的列不为 值的条件称为 空值拒绝 （ ）， 在被驱动 where null reject-NULL
表的 子句符合空值拒绝的条件后，外连接和内连接可以相互转换。 这种转换带来的好处就是优化器可以通过评估表的不同顺序的 where
成本，选出最低成本的连接顺序进行查询。
欢迎一键三连~
有问题请留言，大家一起探讨学习
----------------------Talk is cheap, show me the code-----------------------
MySQL ——cost 成本计算 之 MySQL 如何 选择 使用 索引 (trace) 专注写bug 534

文章目录前言trace工具分析执行计划 前言 在之前博客中，根据测试案例，发现同样的sql语句，因为传入的条件值的不同，导致 MySQL 有时候 选择 使用 索
mysql 优化 器怎么 选择 索引 , 为什么 MySQL 查询 优化 器会 选择 聚集主 索引 上的二级 索引 ？ . . . weixin_35637837的博客 174
为什么 Mysql 优化 器在执行’select * from lookup’而没有order by子句时 选择 二级 索引 . 它只是一个侥幸,或者这是一个幕后 优化 ,假设你添加了一个二级 索引 ,它比
MySQL 查询 为什么 选择 使用 这个 索引 ? ——基于 MySQL 8 . 0 . 22 索引 成本 . . . 7-21
对于InnoDB引擎来说,页是磁盘和内存之间交互的基本单位, MySQL 规定:读取一个页面花费的 成本 默认是1 . 0 ,读取以及检测一条记录是否符合搜索条件的 成本
mysql 执行 成本 _ io cost cpu cost 8-1
第一, mysql 会计算一个页面大概有多少记录,第二 mysql 会计算这两个区间参数大概隔了多少个页面,然后 使用 上EXPLAIN里的rows值大概有几条记录,这里参考
mysql 索引 计算 成本 clcj66885的博客 411
http://www . orczhou . com/index . php/2 0 12/12/ mysql -source-code-optimizer-range-and-ref/ . . .
mysql 索引 的 成本计算 weixin_45504565的博客 931 最新发布
我们之前老说 mysql 在执行一个 查询 时可以有不同的执行方案，他会 选择 其中 成本 最低，或者说代价最低的那种方案去真正的执行 查询 。不过我们之前对 成本
数据库_ mysql 备注 . extra怎么打 7-26
(2) mysql 索引 mysql 索引 数据结构有B+Tree和Hash 索引 两种, 索引 在 mysql 中也叫做键 (1)B+Tree 任何一种数据结构都不是凭空产生的,一定会有它的背景和 使
手撕面试官必备技能- Mysql 内核 成本计算 初探_ mysql 执行 成本 预估-CSDN . . . 7-25
上面提到过 mysql 的 成本 主要是 IO 和CPU。 其中 IO 的 成本 常数 1 . 0 、CPU 成本 常数 0 . 2。 我们首先来分析下全盘扫描的 成本 全盘扫描 成本计算 全盘扫描就是
Mysql 索引 成本计算 规则 Optimizer Trace qq_1149513559的博客 1258
文章目录1 . 开启Optimizer Trace2 . sql 成本 核算规则 MySQL 5 . 6之前的版本只能通过EXPLAIN语句查看到最后 优化 器决定 使用 的执行计划，却无法知道
mysql 8 . 0 . 22 -linux-x 8 6_64 . tar . zip 08-30
这个压缩包“ mysql 8 . 0 . 22 -linux-x 8 6_64 . tar . zip”包含了在 Linux 上安装和运行 MySQL 8 . 0 . 22 所需的所有文件和组件。以下是关于 MySQL 8 . 0 . 22 在 Linux 上
Mysql 性能调优与架构设计- 8 7-19
回 表 成本 = IO IO 成本 + CPU 成本 两 表 连接 成本计算 驱动 表 的扇出 连接 查询 总 成本 = 单次 查询 驱动 表 的 成本 + 扇出数*单次访问被驱动 表 的 成本 多 表 连接 成
MySQL 性能调优(四): MySQL 的执行原理( MYSQL 的 查询 成本 )_ mysql 查询 成本 计 . . . 7-1
1 . 0 、 0 . 2这些数字称之为 成本 常数 ,这两个 成本 常数 我们最常用到,当然还有其他的 成本 常数 。 注意,不管读取记录时需不需要检测是否满足搜索条件,哪怕是空
Mysql 8 . 0 . 22 免安装版本安装包 12-31
1 . **下载与解压**：首先，下载名为" mysql - 8 . 0 . 22 -winx64"的压缩文件，然后在你的计算机上 选择 一个合适的目录进行解压。 2 . **配置环境变量**：为了让系
Mysql 8 . 0 . 22 MAC版本 12-04
- MySQL 8 . 0 . 22 引入了诸多性能 优化 ，如InnoDB存储引擎的改进， 查询 优化 器的升级，以及更快的 索引 构建。 7 . **新特性**： - 包括窗口函数、JSON字段
mysql 优化 必会-什么是 成本 _数据库的 io 成本 是什么 7-27
CPU 成本 :9693* 0 . 2 +1 . 0 =1939 . 6,9693是行数, 0 . 2是访问的 成本 常数 ,1 . 0 是微调值 总 成本 :9 8 . 1+1939 . 6 = 2 0 37 . 7 计算 使用 不同 索引 执行 查询 的代价 1、 使用 u
MySQL 学习整理-码农进阶之路(三) 8-10
IO 成本 [524 8 0 0 *1+1 . 1( 常数 )=524 8 0 1 . 1]+CPU 成本 [64479325* 0 . 2+1=12 8 95 8 66]=1342 0 667 0 1 索引 查询 (需 回 表 ) 成本 (随机 IO ): explain select * from users wh
最新版windows mysql - 8 . 0 . 22 -winx64 12-25
这个最新版的"windows mysql - 8 . 0 . 22 -winx64"是专为Windows操作系统设计的 MySQL 服务器的最新稳定版本。它包含了增强的功能、性能 优化 以及一系列的
mysql - 8 . 0 . 22 -linux-glibc2 . 12-x 8 6_64 . tar . xz 01-14
值得注意的是，“glibc2 . 12” 表 示这个版本的 MySQL 是为兼容GLIBC 2 . 12库编译的，这意味着它可以在 使用 这个版本或更高版本GLIBC的Linux发行版上运行。
mysql 执行计划 成本 分析和慢查解决方案_sql执行计划的 成本 什么样的是慢 . . . 8-8
通过前面的案例,我们可以看到, 查询 数据可以直接在聚簇 索引 上进行全 表 扫描,也可以走二级 索引 扫描后到聚簇 索引 回 表 。看到这里,你不禁要问了, MySQL 到
mysql 8 . 2 0 稳定性_ mysql 8 . 0 . 2 0 优化 器 成本 记录 表 weixin_39743511的博客 154
mysql > select *from engine_cost;+-------------+-------------+------------------------+------------+---------------------+---------+---------------+| engine_name | device_type |
索引 成本计算 和 优化 建议 qq_31142237的博客 1496
trace 成本 分析时可以根据 索引 成本 结果来分析 索引 选择 过程，但是 索引 成本 结果并不是最终执行计划结果，最终的执行计划 成本 best_access_path，会基于
MySQL 索引 基数 走在命运的左岸 8323
MySQL 索引 基数 前提 存储引擎：InnoDB 存储引擎 索引 数据结构：B+Tree 概念 索引 基数（cardinality）： 索引 中不重复的 索引 值的数量； 例如，某个数据
mysql count(*) 使用 索引 和 成本计算 wuhuagu_wuhuaguo的博客 2628
目录 一、 索引 成本计算 1、 IO 成本 2、CPU 成本 二、cout(*) 索引 ---选 成本 最小的辅助 索引 总结 一、 索引 成本计算 在有多个 索引 的情况下， 在 查询 数据前，
mysql 执行计划查看工具explain weixin_30729609的博客 170
在 优化 sql语句时，我们经常会用到explain语句，这里对explain语句做一个详细的总结说明。 The EXPLAIN statement provides informat io n about how MyS
mysql 三高讲解（三）3 . 2：如何 确定 用哪条 索引 weixin_43907175的博客 938
1 . 索引 的 确定 基本原则 实验： 1 . 我们先新建一个 表 ： CREATE TABLE sakila . city_1(city VARCHAR(5 0 ) NOT NULL); 2 . 向里面插入数据： INSERT into sakil
MySQL 5 . 7 cost模型浅析 oraly的博客 2511
代价模型 mysql 5 . 7 . 1 0 cost计算相对之前的版本有5 . 7 代价模型浅析较大的改进。例如 cost模型参数可以动态配置，可以适应不同的硬件 区分考虑数据在内
MySQL 索引 成本 H_L_S的专栏 573
添加 成本 ： DBMS会将 索引 添加至其 索引 键所属的叶子页上，但是该叶子页可能没有足够的空间来存放这个 索引 行，在这种情况下，DBMS将会分裂叶子
MySQL 内核 查询 成本计算 sermonlizhi的博客 1185
在《 索引 优化 (一)》的文章中，提到可以通过trace工具来查看 MySQL 是如何 选择 索引 的。 使用 方式： set sess io n optimizer_trace="enabled=on",end_mark
关于我们 招贤纳士 商务合作 寻求报道 400-660-0108 kefu@csdn.net 在线客服 工作时间 8:30-22:00
公安备案号11010502030143 京ICP备19004658号 京网文〔2020〕1039-165号 经营性网站备案信息 北京互联网违法和不良信息举报中心
家长监护 网络110报警服务 中国互联网举报中心 Chrome商店下载 账号管理规范 版权与免责声明 版权申诉 出版物许可证 营业执照
©1999-2025北京创新乐知网络技术有限公司

