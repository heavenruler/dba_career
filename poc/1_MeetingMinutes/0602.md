# Shard / Rebalance + Cross-region 整合測試規劃（2026-06-02 會議備忘）

> 本檔合併原 0602.md（單區 Track A/B/C/D）+ 0602-2.md（跨區 Track E）。對應對話：CRDB Phase 2 5-cell suite batch 進行中（PID 1119304 @ .31，BATCH_TS=20260601T105859+0800）。
>
> 區分標記：
> - **[已實作事實]** 引用具體 `file:line`
> - **[repo 推論]** 從現有檔案合理推導
> - **[下一步建議]** 需 user 裁示後落地

---

## 1. Repo 現況摘要

**[已實作事實]**：

| 控制點 | 實作位置 | 結論 |
|---|---|---|
| shard / RF / iso 三變數為 vm-3node 必鎖 | `results/PoC-DESIGN.md:62-66` | controlled experiment 原則已寫死 |
| 三家 auto-split 全關 | `ansible/playbooks/tidb-vm3.yml:198-210`、`cockroach-vm3.yml:193`、`yugabyte-vm3.yml:29,38,164,173,216,225` | TiKV `region-split-size=128GB` / CRDB `kv.range_split.by_load_enabled=false` / YBDB `enable_automatic_tablet_splitting=false` |
| 三家 RF 設定走 cluster default | `tidb-vm3.yml:201`（`replication.max-replicas`）、`cockroach-vm3.yml:205`（`ALTER RANGE default ... num_replicas`）、`yugabyte-vm3.yml:271`（`configure data_placement --rf=N`） | RF 為 cluster/database-level，依 sub_topology 注入 |
| 預期 shard 數 per sub-topology | `tests/common/prepare.sh:43-47` | 1s\*r=1, 3s\*r=3, haproxy-3s3r=3 |
| 手動 SPLIT（3s\*r post-prepare） | `prepare.sh:117-165` | TiDB `SPLIT TABLE ... BY (43),(86)`；CRDB `ALTER TABLE ... SPLIT AT VALUES (43),(86)`；YBDB 走 pre-create 路徑 |
| YBDB pre-create SPLIT | `prepare.sh:86-102` | 1s\*r/3s\*r 都 pre-create，`sed s\|SPLIT INTO 1 TABLETS\|SPLIT INTO $EXPECTED_SHARDS TABLETS\|g` 套版 |
| Shard-count hard gate | `prepare.sh:261-335`、PoC-DESIGN §7.5.4 表清單 9 表 | 9 表逐張 query，fail-closed |
| Dry-run anchor 驗 RF/topology/iso | `dry-run-confirm.sh:118-216` | 4 dump txt + `.dry-run.done` JSON `all_pass` |
| TiDB PD schedule limits（**例外**） | `tidb-vm3.yml:204-210` | `leader-schedule-limit=4`、`replica-schedule-limit=4`、`region-schedule-limit=0` — leader/replica 允許背景搬移，region 數凍結 |
| README 主表標 shard/replica 為核心變數 | `results/README.md:170 (註N10)` | shard 或 RF 沒鎖死則跨 cell 不可比 |

**仍是設計或待補（`missing source`）**：

- `pipeline-log-template.md` 沒有 `rebalance / settle window` 欄位（grep `settle` 全 repo 0 命中）
- Makefile 沒有 `scaleout-*` / `rebalance-*` target（grep `scaleout|scale-out|rebalance` 全 repo 0 命中）
- PoC-DESIGN 沒有「steady-state vs scale-out benchmark」獨立章節（§2、§6.4.4 散見但無正式對照口徑）
- 今天 (2026-06-01) 踩到的 **F-D** 顯示 shard-count gate 沒設計 fallback：CRDB v26.2.0 起 `crdb_internal.*` 受限（SQLSTATE 42501），原 query 靜默失敗導致全 9 表 actual=0 → fail-closed；commit `ebc481f` 改用 `SHOW RANGES FROM TABLE`。同類問題於 `dry-run-confirm.sh §1c` 已先 F-A-v2 no-op 化（commit `eaa2420`）。
- **跨區 (Track E)**：iac/ansible 全未支援 6-node 跨區，`iac-gcp/` 目錄存在但配對 ansible playbook 未實作（**[missing source]**，詳 §10）

---

## 2. 三家 DB 設定責任邊界

### 2.1 單區（vm-3node）

| DB | cluster-level | node/service-level | table/schema-level | PoC 正式控制點 | 風險 |
|---|---|---|---|---|---|
| **TiDB** | `pd.replication.max-replicas`（RF）、`pd.schedule.*-schedule-limit`、`coprocessor.region-split-size/region-max-size` | TiKV per-store gflags（storage.block-cache、raftstore.sync-log）；本 repo 三 store 同 spec | `SPLIT TABLE ... BY`（DDL）、`ANALYZE TABLE`、index-level region | **SPLIT TABLE** + `region-split-size=128GB`（凍結 size split）+ `region-schedule-limit=0`（凍結 region 搬移）；leader/replica rebalance **保留 4**（D10/Fix #11 設計） | TiKV per-node 異參會破壞對照（IDC 必同 spec） |
| **CockroachDB** | cluster setting：`kv.range_split.by_load_enabled`、`sql.txn.read_committed_isolation.enabled`；`ALTER RANGE default CONFIGURE ZONE` | per-node start flag（`--cache`、`--max-sql-memory`、`--store`、`--locality`） | `ALTER TABLE ... SPLIT AT VALUES`、`ALTER DATABASE ... CONFIGURE ZONE` 子層、`CREATE STATISTICS` | **SPLIT AT VALUES** + `kv.range_split.by_load_enabled=false` + `num_replicas=N`；無 region scheduler 等同物 | `--locality` 異 zone 會驅動 zone-aware placement → 與 sharding 變數混淆 |
| **YugabyteDB** | tserver gflags：`ysql_num_shards_per_tserver`、`enable_automatic_tablet_splitting`、`yb_enable_read_committed_isolation`；`yugabyted configure data_placement --rf` | per-tserver gflags（如 `memory_limit_hard_bytes`、`durable_wal_write`） | `CREATE TABLE ... SPLIT INTO N TABLETS`、`ALTER TABLE ... SPLIT INTO` | **SPLIT INTO N TABLETS** at CREATE + `enable_automatic_tablet_splitting=false` + `--rf=N` | RF=1 placement 只覆蓋 1 tserver（PoC §7.5.3 註：2026-05-23 修正），3s\*r 仍須顯式 SPLIT |

> **[repo 推論]** 三家共同模式：**SPLIT 是 schema-level DDL，RF 是 cluster/database-level cluster setting**；只有 TiDB 把 `region-split-size` 放在 TiKV node config，但 cluster 內三 store 同 spec → 等價於 cluster-level。

### 2.2 跨區（Track E 用，6-node IDC + GCP）

| DB | placement label / locality 設定 | leader-affinity 控制 |
|---|---|---|
| **TiDB** | PD `replication.location-labels=region,zone` + tikv start `--labels region=idc/gcp,zone=z1/z2/z3` | `label-property reject-leader region=gcp`（強制 leader 留 IDC）；或 PD `leader-priority`、`leader-schedule-limit` 配 region |
| **CRDB** | per-node `--locality region=idc,zone=z1/z2/z3` + `ALTER ... CONFIGURE ZONE USING lease_preferences='[[+region=idc]]', voter_constraints='{+region=idc:2, +region=gcp:1}'` | 跨區語義最完整；可 `voter_constraints` 精控每 shard voter 分布 |
| **YBDB** | `yugabyted start --placement_region=idc --placement_zone=z1` + `yb-admin modify_placement_info` + `set_preferred_zones` | `set_preferred_zones` 鎖 leader region |

**[repo 推論]** 目前三家 playbook **未注入** locality / placement label；Track E 必須補。

---

## 3. 問題回答（單區）

### Q1 IDC / GCP 能否拆不同設定？

**TiDB**
- **[已實作事實]** PD/TiKV/TiDB-server 可分機架部署，PD `replication.location-labels` + tikv labels 可做 zone-aware placement，但本 PoC 三 TiKV 同 spec（`tidb-vm3.yml:198-210` 無 per-store 差異），未啟用 zone label。
- **[repo 推論]** 若 IDC ↔ GCP 一邊 `region-split-size=128GB`、另一邊 default 96MB，rebalance / split 行為**不可比**，會把跨環境差異混入 shard 變數 → **應禁止異參**。

**CockroachDB**
- **[已實作事實]** `--locality=region=...,zone=...` 是 per-node start flag，CRDB 會依 zone config 自動 zone-aware；本 PoC `cockroach-vm3.yml:205` 只動 cluster `num_replicas`，未設 locality。
- **[repo 推論]** IDC vs GCP 異 locality 會啟動跨地域 placement → 違反「controlled experiment shard 鎖定」原則；只能作 exploratory 拓樸（e.g. multi-region）。

**YugabyteDB**
- **[已實作事實]** tserver gflags 可 per-node 設，但 placement 由 yugabyted/yb-master 統一決定；本 PoC `yugabyte-vm3.yml:38,173,225` 三 tserver flags 一致。
- **[repo 推論]** 跨 IDC tserver 異 `ysql_num_shards_per_tserver` 會直接破壞 tablet 預期數計算（§7.5.3 公式 `ysql_num_shards_per_tserver × num_tservers_in_placement`）；**禁止異參**。

**結論**：三家**可拆**（DB 機制本來就提供），但對 PoC 對照**不應拆**，否則 tpmC/p99 來源無法 attribute。**Track E (§10) 是受控異 locality 場景**，作 exploratory 獨立成 chapter。

---

### Q2 cluster 設 1 shard 再 split 成 3 shard 可行？

**TiDB**
- **[已實作事實]** `prepare.sh:134-144` 已實作 `SPLIT TABLE ... BY (43),(86)` 把每張表切兩刀。可行；hard gate `prepare.sh:275-280` 驗 region_count==3。
- 踩坑（`prepare.sh:124-133` 註）：CLUSTERED PK 表不能用 `INDEX PRIMARY`、`BETWEEN/REGIONS` 對 warehouse 觸發 ERROR 8212；改用顯式分裂點 mirror CRDB。

**CockroachDB**
- **[已實作事實]** `prepare.sh:147-157` 已實作 `ALTER TABLE ... SPLIT AT VALUES (43),(86)`。可行；hard gate 走 `SHOW RANGES FROM TABLE`（今天 F-D 修，原 `crdb_internal.ranges` v26.2.0 受限，commit `ebc481f`）。

**YugabyteDB**
- **[已實作事實]** 不走 post-prepare SPLIT，走 **pre-create with SPLIT INTO N TABLETS**（`prepare.sh:96-102`）。可行；hard gate 用 `yb-admin list_tablets`。
- 註：v2025.2 也有 `ALTER TABLE ... SPLIT INTO` 但本 PoC 未用（pre-create 是穩定路徑）。

**結論**：**三家皆可行**，且**已是本 PoC 正式 baseline 路徑**。

---

### Q3 cluster 設 3 shard 不做 manual split 可行？

**TiDB**
- **[repo 推論]** 沒有 cluster-level「預設 N regions per table」設定；regions 由 TiKV 依 `region-split-size`（128MB default）自然 split。在 `region-split-size=128GB`（本 PoC 設定）下，128W 的 TPC-C 表（最大 order_line ~3GB）不會自然 split → 預設只 1 region/表。若改 `region-split-size=1MB` 強制自然分裂 → 數量不可控（隨資料 byte size），且會在 run 期間繼續 split → 污染。
- **不可行**作 controlled baseline；只能作 exploratory。

**CockroachDB**
- **[repo 推論]** `range_max_bytes=137438953472`（128GB，PoC-DESIGN §7.5.2 line 634）下 TPC-C 表也不會自然 split。開 `kv.range_split.by_load_enabled=true` + load-based split 可能在 run 期間因 QPS 高的 hot range 動態切分 → 同 TiDB 不可控。
- **不可行**作 controlled baseline；只能作 exploratory。

**YugabyteDB**
- **[已實作事實]** §7.5.3 line 684 註明：「3 tservers × ysql_num_shards_per_tserver=1 = 3 自然 tablets」**錯**，2026-05-23 cell 3 dispatch 實測 RF=1 placement 只覆蓋 1 tserver → 自然 1 tablet。即使 RF=3 下「自然 3 tablets」也只在 cluster default 路徑成立，但這仍是 cluster gflag 結果，不是「不 split」的零控制。
- **不可行**作 controlled baseline；3s\*r 須 pre-create + SPLIT INTO。

**結論**：**三家皆不可行**作 controlled baseline。理由：自然 shard 數隨資料量/負載動態變化，跨 cell/cross-DB 不同步。

---

### Q4 auto rebalance / auto sharding 對 TPC-C 影響？

**共同影響（[repo 推論]）**：
- run 期間若觸發 split → 寫熱點 range/region/tablet 進入 split → tail latency p99 飆升、temporary unavailability、retry rate 上升
- 若 rebalance（leader / leaseholder / tablet leader move）→ followers re-snapshot、cache thrashing → tpmC 下降
- benchmark 期間任一 trigger → 該組 N=1 結果**不可作正式 median**（README §註N10 已寫 shard/replica 沒鎖死則不可比）

**TiDB 特例**：
- **[已實作事實]** `tidb-vm3.yml:208-210` 故意保留 `leader-schedule-limit=4` + `replica-schedule-limit=4`、只關 `region-schedule-limit=0`。意圖（Fix #11 D10 註）：region 數凍結（鎖 shard 變數），但 leader/replica 允許搬移（RF=3 部署需要 PD 主動 make-up-replica）。
- **[repo 推論]** 這實際上**允許 run 期間 leader balance** — 是「**part-controlled**」狀態，不是純 steady-state。dispatch records `2026-05-31-tidb-schedule-limit-0-vs-4` 即比較 l0r0 vs l4r4 兩種 PD 設定 → 顯示 leader balance ON 對 tpmC 有 +5~10% 影響。

**結論**：auto rebalance / split **必須**在正式 baseline 期間全關；TiDB l4r4 屬於「**已知 caveat 的混合狀態**」，需在主表標明。

---

## 4. 建議測試矩陣（最小可行）

| Track | Topology | Shard 控制 | Rebalance 控制 | 用途 | 是否納入正式 README 主表 |
|---|---|---|---|---|---|
| **A. controlled baseline** | vm-3node-{1s1r,1s3r,3s1r,3s3r,haproxy-3s3r} × RC | manual SPLIT + hard gate（**現行**） | TiDB l4r4 / CRDB by-load off / YBDB tablet-split off | 正式 tpmC/p99 median，跨 DB 對照 | ✅ 是 |
| **B. default-shard exploratory** | vm-3node-3s\*r-default-RC | 不做 manual SPLIT，靠 cluster gflag 自然產生 | 同 A | 量化「manual split 與 default 之差」 | ❌ 否（caveat 段） |
| **C. auto-split exploratory** | vm-3node-3s3r-autosplit-RC | manual SPLIT 保底 | TiDB l4r4 + region-split-size 預設 96MB；CRDB by-load ON；YBDB tablet-split ON | 量化「run 期間 auto-split 對 p99/error rate 影響」 | ❌ 否（caveat 段） |
| **D. scale-out exploratory** | vm-3node-3s3r-RC → add 1 node mid-run | manual SPLIT 起跑 | rebalance ON（必要） | 量化「擴容期 OLTP 衝擊」 | ❌ 否（獨立 chapter） |
| **E. cross-region (跨專線)** | vm-6node-3s3r-haproxy-RC（IDC×3 + GCP×3）| manual SPLIT + hard gate | placement 鎖定（P-A majority IDC / P-B 平衡）；leader-affinity gate | 量化「跨專線後 raft 同步效率 / 兩區並發效能 / chaos 失效」（詳 §10） | ❌ 否（獨立 chapter） |

**矩陣設計原則**：
- 每 track 跑 1 個 cell（**不**全 cell 重複），用 3s3r 作主代表（負載/變動最大）
- Track A 已基本完成（YBDB 全綠、TiDB N=1、CRDB 進行中）；B/C/D/E 為新增 exploratory phase

---

## 5. 執行順序

| Phase | 目的 | 指令或需新增 target | 預期 artifact | 是否正式數據 |
|---|---|---|---|---|
| **P1**（**進行中**） | 完成 Track A CRDB 5-cell suite | 現行 `/tmp/batch-crdb-5cell-suite.sh`（已 dispatch 1119304） | `results/crdb-tc1/S-BASE/vm-3node-*-rc/` | ✅ 正式（N=1，需 N=3 才入主表） |
| **P2** | 完成 Track A TiDB 5-cell suite | `/tmp/batch-tidb-5cell-suite.sh`（已寫好同步 .31，待 dispatch） | `results/tidb-tc1/S-BASE/vm-3node-*-rc/` | ✅ 正式 |
| **P3** | Track B default-shard exploratory（1 cell × 3 DB） | 新增 Makefile `vm3-<db>-3s3r-default-rc`（拓樸名加 `-default` 後綴，prepare.sh 加 `--no-split` arg） | `results/{db}-tc1/S-BASE/vm-3node-3s3r-default-rc/` | ❌ caveat |
| **P4** | Track C auto-split exploratory | 同 P3 + ansible playbook 變體（auto-split ON）`vm3-<db>-3s3r-autosplit-rc` | `results/{db}-tc1/S-BASE/vm-3node-3s3r-autosplit-rc/` + rebalance trace | ❌ caveat |
| **P5** | Track D scale-out exploratory | 新增 Makefile `scaleout-vm3-<db>-3s3r-rc`，run 中 SSH 一個 add-node 動作 | `results/{db}-tc1/S-BASE/vm-3node-3s3r-scaleout-rc/` | ❌ 獨立 chapter（建議考慮整合進 Track E §10） |
| **P6** | 補正式 README 章節 | 更新 `PoC-DESIGN.md` 新增 §6.5 「steady-state vs exploratory tracks」、`README.md` 新增 exploratory 子表 | docs only | — |
| **P7–P10** | Track E 跨區 PoC（Pre-P0 + P0–P4，詳 §10.6） | 6-node ansible playbook + 跨區 placement gate + WAN baseline + chaos infra | `results/{db}-tc1/X-CROSS/...` + `wan/*` + `chaos/*` | ❌ 獨立 chapter |

---

## 6. Gate / Artifact 要求

**每組（A/B/C/D/E）必留**：

| 項目 | 路徑 | A baseline | B/C exploratory | D scale-out | E cross-region |
|---|---|---|---|---|---|
| shard-count | `prepare/shard-count.txt` | ✅ hard gate | ✅ snapshot only | ✅ pre + post | ✅ hard gate |
| RF actual | `dry-run/replication-factor.txt` + dry-run gate | ✅ hard gate | ✅ | ✅ | ✅ |
| isolation gate | `gate/isolation-db.txt` + `gate/isolation-driver.txt`（YBDB triple gate） | ✅ | ✅ | ✅ | ✅ |
| rebalance trace | **新增**：`run/rebalance-trace.json`（per-DB scheduler dump） | – | ✅ 必要 | ✅ 必要 | ✅ 必要 |
| go-tpc stdout | `runs/threads-*/round-*/go-tpc-stdout.txt` | ✅ | ✅ | ✅ | ✅ |
| summary.json | `summary.json`（由 `summary-from-stdout.py` 生成） | ✅ | ✅ | ✅ | ✅ |
| DB-host metrics | `runs/.../mpstat-db.txt`、`iostat-1s-db.txt`、`vmstat-1s-db.txt`、`sar-net-db.txt` | ✅（**目前只採 CLUSTER_HOST 一台，三 node 採集缺口已知**） | ✅ | ✅ 含 add-node 前後 | ✅ **必須**多 node fan-out（含 GCP 三 node） |
| settle window 紀錄 | **新增**：`prepare/settle-window.txt` 記載 quiesce 時間、rebalance complete 時間 | – | ✅ | ✅ 必要 | ✅ 必要 |
| WAN baseline | **Track E 專用**：`wan/baseline-rtt.txt`、`wan/baseline-bw.txt`（iperf3 + ping 起前/起末 60s） | – | – | – | ✅ 必要 |
| WAN runtime trace | **Track E 專用**：`wan/runtime-bytes.txt`（ifstat IDC↔GCP 介面，每秒） | – | – | – | ✅ 必要 |
| placement actual | **Track E 專用**：`placement/leader-region.txt`、`placement/voter-region.txt` | – | – | – | ✅ hard gate |
| chaos timeline | **Track E 專用**：`chaos/<C-id>/timeline.json`、`tpmc-1s.txt`、`error-rate-by-sec.txt`、`leader-redist-trace.txt` | – | – | – | ✅（僅 Test 3） |

**新增建議 gate**：

| Gate | 條件 | 三家對應指令 |
|---|---|---|
| **rebalance complete** | `under_replicated_ranges==0` 連續 N 秒 | TiDB: `pd-ctl region check miss-peer/extra-peer`；CRDB: `SHOW CLUSTER RANGES`（v26.2.0；舊 `crdb_internal.ranges_no_leases` 已淘汰）；YBDB: `yb-admin get_load_move_completion` |
| **leader/leaseholder settle** | leader 分布 stddev < 閾值 | TiDB: `information_schema.tikv_region_status` group by leader_store_id；CRDB: `SHOW RANGES FROM DATABASE ... WITH DETAILS`；YBDB: `yb-admin list_tablet_servers` + tablet leader count |
| **shard/tablet/range-count** | **已實作**（`prepare.sh:261-335`） | 同 §7.5.4 |
| **post-scale-out settle window** | add-node 後等 X 秒；X 由 cluster size 決定 | universal sleep + 上述 gate |
| **placement leader-affinity**（**Track E 新增**） | actual leader region == expected（依 P-A / P-B） | TiDB: pd-ctl scheduler show；CRDB: SHOW RANGES FROM DATABASE WITH DETAILS（包 lease_region）；YBDB: yb-admin list_tablet_servers + tablet leader location |

---

## 7. Critical Risks（讓結果不可引用）

1. **shard 數鎖定失效**（hard gate 沒 catch）→ 跨 cell 對照無效（README 註N10）
2. **run 期間 auto-split/rebalance trigger 但無 trace**（invalid mixed run）→ tpmC 來源無法 attribute
3. **三家 isolation 對齊失效**（go-tpc `--isolation` 沒走 conn-params）→ PoC-DESIGN §7.4 已有 two-layer gate 保護
4. **DB-host metrics 只採單台 .32**（本對話已暴露 gap，`run.sh:63-65` `CLUSTER_HOST` 寫死）→ 3-node placement skew 無法觀察；正式 baseline 需補多 node 採集，否則跨 sub-topology 比較有 hole。**Track E 必須補 fan-out 採集**（user 在純 IDC 場景表態「forget it」，但跨區無 GCP metrics 等於整組數據不可解讀）
5. **TiDB l4r4 屬已知 caveat**（leader/replica rebalance ON）→ 正式主表須明標
6. **N=1 不可作對外結論**（README 註N9）→ 三家 vm-3node 都要 N=3
7. **v26.2.0 `crdb_internal.*` access restricted**（今天踩到 F-D，commit `ebc481f`）→ 同類 `dry-run-confirm.sh §1c` 已 no-op（F-A-v2，commit `eaa2420`）；其他散落呼叫尚未稽核
8. **Track E：iac/ansible 全未支援 6-node 跨區**（GCP playbook 未實作）→ Pre-P0 預估 **2–3 天** 補
9. **Track E：WAN 變數不可控**（GCP region 選擇 / IDC 出口 / 路由跳數 / 運營商 QoS）→ per-test 起前/起末各測 60s baseline，作 caveat 寫主表
10. **Track E：iso=rc + 跨 WAN raft**：平均寫 latency = round-trip 跨區；若 RTT > 50ms，Test 1 tpmC 預期 < 30% baseline → 預先估算 RTT × 寫 % → 評估是否值得跑
11. **Track E：go-tpc retry 行為**：fail-over 期 error 統計不準（不知是「拒絕」還是「重試成功」）→ chaos 場景額外採 `error-rate-by-sec.txt` 區分
12. **Track E：placement actual ≠ config**（PD/allocator 遲遲不調度）→ 新 hard gate `placement/leader-affinity-gate` fail-closed
13. **Track E：cost 爆炸**：6-node × 3 DB × 2 placement × 3 test + chaos = 太多 → 先 1 DB（CRDB）全套；TiDB/YBDB 範圍依結果決定

---

## 8. 建議結論（單區）

| 問題 | 結論 |
|---|---|
| 下一階段是否維持 manual split 作正式 baseline？ | **是**。三家已有完整 SPLIT + hard gate 路徑（§7.5），且 default-shard 路徑被 §7.5.3 line 684 註明對 RF=1 不成立、CRDB/TiDB 在 `range_max_bytes=128GB` 下自然不 split。**正式 baseline = manual split + auto-split OFF + leader/replica scheduler 全關**（**含 TiDB l4r4 需移至 caveat 或獨立 cell**）。 |
| default shard / auto split 是否只能作 exploratory？ | **是**。Track B（default）與 Track C（auto-split ON）獨立成 exploratory，產出 `caveat 段` 與正式主表並列、**不計入跨 DB median**。 |
| 是否需要新增 PoC-DESIGN 段落？ | **是**。新增 **§6.5「Track 分類：controlled baseline vs exploratory tracks」**，明訂 A/B/C/D/E track 定義、必選 gate、納入主表/caveat 的判定。 |
| 是否需要新增 Makefile target？ | **是**。新增 `vm3-<db>-3s3r-default-rc`、`vm3-<db>-3s3r-autosplit-rc`、`scaleout-vm3-<db>-3s3r-rc`、`vm6-<db>-3s3r-haproxy-rc` 四組（exploratory 不走 5-cell batch，獨立 dispatch）。 |
| 是否需要新增 pipeline-log-template 欄位？ | **是**。`Phase 階段時序` 後加 **「Rebalance / Settle 狀態欄」**：起終時間、`under_replicated_count` 變化、leader stddev 變化。 |

---

## 9. 水平擴充與 re-sharding / re-balance 規劃

| DB | scale-out 觸發行為 | auto re-sharding / rebalance 常見行為 | 對 TPC-C 指標影響（推論） | 正式 benchmark 措施 | exploratory 測項 |
|---|---|---|---|---|---|
| **TiDB** | add TiKV → PD detect → start make-up-replica + leader balance | 受 `region-schedule-limit` / `leader-schedule-limit` / `replica-schedule-limit` 控制；本 PoC l4r4 模式下 leader/replica 持續搬，region 數凍結 | leader move → 新 leaseholder cache 冷啟 → 短期 p99 飆升、tpmC 下降 5-15%；replica add → 寫 raft 多一份 quorum cost | **設 0** 後跑（l0r0），run 後再復原為 l4r4；或於 add-node 後等 `under_replicated==0` settle 至少 N min | l4r4 下 add-node、觀察 leader move 對 t=128 p99 影響 |
| **CockroachDB** | add node → allocator detect → start replica rebalance + lease move | 預設開（無 cluster setting 全關 rebalance）；可 `SET CLUSTER SETTING kv.allocator.load_based_lease_rebalancing.enabled=false` 部分關 | leaseholder move → 該 range 短期 read latency double、write retry 上升；replica add → quorum cost +33% per range during settle | run 期間需 settle 至 `under_replicated==0` + leaseholder stddev < 閾值；不允許 add-node 在 run 中 | add-node 在 run 第 3 round 後注入，觀察 retry/p99 |
| **YugabyteDB** | add tserver → yb-master detect → tablet load balancer 開始搬 leader/tablet | 受 `--enable_load_balancing` 控制（預設 ON）；本 PoC 已關 `enable_automatic_tablet_splitting`，但 **load_balancing 未顯式設**（`yugabyte-vm3.yml` grep 不到 `enable_load_balancing` → `missing source`） | leader balance → 該 tablet 短期 latency；tablet move → 全 range 重新 snapshot | 必須等 `yb-admin get_load_move_completion` 100% + `yb-admin get_universe_config` placement_blocks 穩定 | add-tserver 在 run 中注入，觀察 tablet leader balance window |

**Track 區分（必須在 SSOT 寫死）**：

| Track | 定義 | 範例 |
|---|---|---|
| **steady-state benchmark**（正式） | 部署 deploy → wait settle → prepare → quiesce 5min → ANALYZE → run；run 期間 placement 不變、scheduler 控制嚴格 | Track A（現行） |
| **scale-out benchmark**（exploratory） | 部署 N-node → run round 1-2 穩態 → add 1 node → run round 3-5 觀察衝擊 | Track D |
| **cross-region benchmark**（exploratory） | 6-node 跨區部署 → placement 鎖定 → IDC-only run / 兩區並發 / chaos 失效 | Track E（§10） |
| **invalid mixed run** | run 期間非預期觸發 split/rebalance + 無 trace/gate 可解釋 | TiDB l4r4 若沒先量 leader balance 收斂；CRDB 沒關 by-load-split |

**Gate 建議**：

1. **rebalance complete gate**（new）：`prepare` 結束加 sample loop 1Hz × 60s，要求 `under_replicated==0` + `pending_replicas==0` 連續 30s
2. **leader/leaseholder settle gate**（new）：leader 跨 store 分布 stddev < 30% of mean，連續 30s
3. **shard/tablet/range-count gate**（**已實作**，`prepare.sh:261-335`）
4. **post-scale-out settle window**（new）：scale-out 測項專用，add-node 後強制等待 `get_load_move_completion==100%` + 額外 60s buffer
5. **DB-host metrics**：目前只採 CLUSTER_HOST（.32），**Track A 現有 gap**（已知，本對話揭露 `run.sh:63-65`）；建議補 fan-out 三 node ssh（user 已表示 forget 不動）；**Track E 必須補**（無 GCP 採集則整組失意義）
6. **DB-internal metrics**：TiDB Grafana scrape `pd_region_*`、CRDB `_status/vars` 主要 metrics、YBDB `:9000/varz` 加 `:9000/metrics`
7. **placement leader-affinity gate**（new，Track E 用）：actual leader region == expected

---

## 10. 跨區 PoC（Track E）— 詳細設計

### 10.0 拓樸基本盤

**[user 提供事實]**

| 區 | TPC-C client | HAProxy | Cluster node1 | Cluster node2 | Cluster node3 |
|---|---|---|---|---|---|
| **IDC** | 172.24.40.31 | 172.24.47.20 | 172.24.40.32 | 172.24.40.33 | 172.24.40.34 |
| **GCP** | 10.162.0.11 | 10.162.0.12 | 10.162.0.13 | 10.162.0.14 | 10.162.0.15 |

**鎖定變數**：iso=rc、haproxy（每區 1）、3 shard × 3 replica

**「不跨區存取」解讀**：應用層必須遵守（IDC client → IDC haproxy → IDC nodes；GCP 同理）；**DB 內 raft / replication 跨 WAN 才是測量對象**。

### 10.1 Placement 機制（詳 §2.2）

`region/zone` label 注入是 Track E 與其他 Track 最大差別。三家 deploy 階段必補 locality，dry-run 階段必補 placement actual gate。

### 10.2 Test 1 — IDC 獨立 TPCC（raft 跨 WAN 對單側寫的損耗）

| 項 | 設定 |
|---|---|
| 拓樸 | 6-node single cluster，placement = **P-A**（每 shard 2 IDC voter + 1 GCP voter；leader 鎖 IDC） |
| Workload | 只 IDC client (172.24.40.31) → IDC haproxy (172.24.47.20) → IDC nodes (.32/.33/.34) |
| GCP 側 | haproxy/client idle；GCP 三 nodes 純當 follower voter |
| Iso | rc |
| W | 128 |
| Thread sweep | 同現行 16/32/64/128（或 user 指定） |
| Rounds | 5（與 vm-3node 對齊） |
| 度量 vs baseline | tpmC、p99、commit latency、raft propose latency、inter-region bytes |
| Baseline 對照 | 現行 CRDB/TiDB/YBDB `vm-3node-haproxy-3s3r-rc` 主表 |
| 預期 | iso=rc + raft 寫需 quorum；majority 在 IDC → 兩 IDC voter ACK 即可 commit，**GCP voter 不擋 critical path**；tpmC ↓ 10–30%（依 RTT） |

**Critical**：若 placement actual 沒落地（GCP 變 leader） → write critical path 跨 WAN → tpmC 預期跌至 < 10% baseline。**[下一步建議]** dry-run-confirm 必須新增 placement actual gate。

### 10.3 Test 2 — IDC + GCP 同時 TPCC（互相干擾 + 跨區並發）

| 項 | 設定 |
|---|---|
| 拓樸 | 6-node single cluster，placement = **P-B**（leader 平衡：1 IDC + 1 GCP + 1 arbiter；或 per-shard leader 各區散） |
| Workload | IDC client + GCP client **並行** |
| 度量 | 兩側獨立 tpmC、合計 throughput、相互 p99 影響、WAN BW 使用率、cross-region key conflict |
| 預期 | WAN BW 競爭 + raft 跨區 leader → **兩側都比 Test 1 差** |
| 衝突隔離 | 建議 W 區分（IDC W=1-64, GCP W=65-128），讓「WAN 互擾」與「key conflict」可分開歸因 |

### 10.4 Test 3 — Chaos / Failover 7 場景

| # | 場景 | 注入方式 | 度量 | 預期 |
|---|---|---|---|---|
| **C1** | IDC cluster node1 down | `systemctl stop <db>` on .32 | tpmC 谷期、error rate、leader 重選 RTO | TiDB ~20–30s / CRDB ~9s / YBDB ~5–15s |
| **C2** | IDC haproxy down | `systemctl stop haproxy` on .20 | client 連線 fail / fallback 行為 | 純應用層失效；DB layer 不受影響 |
| **C3** | WAN 全斷（IDC ⇄ GCP） | `iptables -A INPUT -s 10.162.0.0/24 -j DROP` × 雙向 | majority/minority 行為、split-brain 防護 | P-A：IDC 仍 majority 可寫；GCP minority read-only。P-B：兩區皆 minority → 都寫不通 |
| **C4** | WAN 注入 +200ms 延遲 | `tc qdisc add dev <iface> root netem delay 200ms` | tpmC 漸降、raft heartbeat 失敗閾值 | tpmC 線性下降至 RTT 主導 |
| **C5** | WAN packet loss 5% | `tc qdisc add ... netem loss 5%` | retransmit ↑、retry rate、tail latency | — |
| **C6** | 慢 disk (IDC node1) | `cgroup io.max` 或 `fio` 背景 inflate iops | 該 node leader 主動卸下、tpmC 受擊 | TiDB/CRDB/YBDB 各有 hot-spot 偵測 — 觀察 settle |
| **C7** | IDC 全 3 node down（region majority loss） | `systemctl stop` × 3 同時 | P-A 下 → GCP 剩 1 voter，全 read-only / 寫拒 | placement 驗證關鍵 |
| **C8** (可選) | clock skew injection | NTP off + `date -s` ±2s on .32 | CRDB hybrid logical clock 嚴測 | CRDB 可能拒 transaction；TiDB/YBDB 較不敏感 |

**Chaos 共同 metric artifact**：
- `chaos/<C-id>/timeline.json`（注入 t0、谷期 t1、recover t2、recovery RTO=t2−t0）
- `chaos/<C-id>/tpmc-1s.txt`（per-second tpmC 取樣）
- `chaos/<C-id>/error-rate-by-sec.txt`（區分 conn-refused / timeout / app-retry-success）
- `chaos/<C-id>/leader-redist-trace.txt`（leader 重選歷程）

### 10.5 跨區 artifact tree

```
/tmp/poc-tpcc/artifacts/{db}-region-{test1|test2|chaos-Cx}-rc-<TS>/
├── env/
├── wan/
│   ├── baseline-rtt.txt
│   ├── baseline-bw.txt
│   └── runtime-bytes.txt
├── placement/
│   ├── leader-region.txt
│   ├── voter-region.txt
│   └── .leader-affinity-gate.done
├── gate/                       # 同現行
├── dry-run/                    # 同現行 + 跨區 leader-affinity gate
├── prepare/                    # 同現行
├── runs/
│   └── threads-N/round-N/
│       ├── go-tpc-stdout.txt
│       ├── mpstat-db.txt       # IDC node + GCP node 各一份（fan-out）
│       ├── iostat-1s-db.txt
│       ├── sar-net-db.txt
│       └── vmstat-1s-db.txt
└── chaos/                      # 僅 Test 3
    └── <C-id>/
        ├── timeline.json
        ├── tpmc-1s.txt
        ├── error-rate-by-sec.txt
        └── leader-redist-trace.txt
```

### 10.6 排程

| 階段 | 內容 | 預估 |
|---|---|---|
| **Pre-P0** | (a) iperf3/ping 跨專線 baseline；(b) GCP 環境準備（5 VM 起、SSH 通、disk 預配）；(c) ansible 6-node playbook 重寫（含 locality label）；(d) inventory `vm6.ini` 新增 | **2–3 天** |
| **P7** (P0 of Track E) | placement deploy + dry-run（三家，不跑 prepare/run）驗 §6 gate 落地 | **1 天** |
| **P8** (P1 of Track E, Test 1) | 三家 × P-A × IDC-only 跑 | **3 DB × 3–4h × N=1 = 12h** |
| **P9** (P2 of Track E, Test 2) | 三家 × P-B × 兩側並行 | **同 12h** |
| **P10** (P3 of Track E, Test 3) | 三家 × 7 chaos 場景 | **21 場景 × ~1h = 21h** |

**Track E 總預估**：Pre-P0 + P7..P10 ≈ **6–7 天**（不含 N=3 補測）

---

## 11. 對話脈絡備忘（2026-06-01 → 2026-06-02）

**本日交付**：
- `eaa2420` fix: dry-run-confirm F-A-v2 + F-B-v2 — CRDB 5-cell dry-run validated
- `db3936b` feat: status-vm1.sh — show in-progress phase sub-log
- `ebc481f` fix: prepare.sh F-D — CRDB shard-count gate v26.2.0 supported API
- `e8216f2` udpate — 0602.md 初版
- `412eb83` docs: 0602-2.md（已合併入本檔，0602-2.md 即將移除）

**進行中**：
- CRDB Phase 2 5-cell suite batch（PID 1119304 @ .31，BATCH_TS=20260601T105859+0800）— Cell 1/5 threads-32 round-5
- TiDB 5-cell suite batch 腳本 `.31:/tmp/batch-tidb-5cell-suite.sh` ready（5975 bytes，syntax OK），等 CRDB 完 dispatch
- 預估 5 cell × ~3-4h = ~15-20h
- Monitor `bgsilcyqq`（persistent）即時推進度事件

**已知 gap（未動）**：
- run.sh DB-host metrics 只採單台 .32（user 已表示「nope, forget it」，本次不補；**Track E 必須補**）
- pipeline-log-template.md 欠 rebalance/settle 欄位
- Makefile 欠 exploratory + scale-out + cross-region target
- PoC-DESIGN.md 欠 §6.5 steady-state vs exploratory tracks 章節
- iac/ansible 全未支援 6-node 跨區（Track E 阻塞點）

---

## 12. 待確認事項（合併統整）

### A. 單區 Track A–D 落地決策

| # | 議題 | 我的建議 | 為何 |
|---|---|---|---|
| **A1** | §5 P3–P6 是否落地？ | **P3+P6 落地 / P4+P5 緩** | P3+P6 成本低、補 caveat 完整度；P4+P5 scope 大、ROI 偏 exploratory；P5 建議考慮整合進 Track E §10 |
| **A2** | TiDB l4r4 caveat 是否標 README 主表？ | **標** | dispatch records `2026-05-31-tidb-schedule-limit-0-vs-4` 已證 l4r4 對 tpmC ±5–10%，不標等於誤導 |
| **A3** | TiDB l4r4 是否乾脆改 l0r0 重跑（消滅 caveat）？ | 視 A2 結果 | 若 A2 接受 caveat 即可，不必重跑；若 A2 否決 caveat → 重跑成本 ~3h × 5 cell |
| **A4** | `/tmp/batch-crdb-5cell-suite.sh` + `/tmp/batch-tidb-5cell-suite.sh` 是否搬進 `poc/tests/batch/` 正式位置？ | **搬+commit** | 目前 transient，無 git 留底；若 batch 結構穩定建議入庫 |
| **A5** | v26.2.0 `crdb_internal.*` 散落呼叫稽核要不要做？ | **做**（5–10 min，獨立可平行）| 同類 silent fail 風險未根除 |
| **A6** | N=1 → N=3 何時補？ | **Track A 跑完→挑代表 cell N=3** | 跨 DB median 必要；建議 3s3r 各家補 N=3 |

### B. 跨區 Track E 必答決策（§10）

| # | 議題 | 選項 | 我的建議 |
|---|---|---|---|
| **B1** | 一個 cluster 跨 6 nodes，或兩個獨立 cluster + 邏輯複製？ | A. single 6-node; B. two 3-node + CDC | **A — single 6-node**；測 raft 跨 WAN 是 PoC 重點 |
| **B2** | RF=3 placement 策略 | P-A: 2-IDC + 1-GCP；P-B: 1-IDC + 1-GCP + 1-arbiter | **兩拓樸都測**：P-A 給 Test 1 / P-B 給 Test 2 |
| **B3** | DB 範圍 | TiDB/CRDB/YBDB 全 / 哪一家 | **全 3 家**；若預算緊縮 → **先 CRDB**（跨區語義最完整） |
| **B4** | WAN RTT / bandwidth 已測？ | 必要 baseline | **必須先測**：iperf3 + ping p50/p99 |
| **B5** | N 數 | N=1 / N=3 | **N=1 先**，趨勢確認後挑代表 cell N=3 |
| **B6** | TPCC W | 沿用 W=128 / 變 | **沿用 W=128** |
| **B7** | 是否與現行 IDC 單區 `haproxy-3s3r` 結果對照？ | 是 / 另跑純 IDC 6-node 作 baseline | **是**；作 跨專線前/後 delta |

### C. 跨區開放問題

1. **GCP IP `10.162.0.x` 是否已開通 IDC ↔ GCP 路由？**（決定 Pre-P0 起點）
2. **預算與時間**：是否接受 Pre-P0 2–3 天 ansible 重寫？或 GCP 端 manual deploy 先測？
3. **DB 優先序**：先 CRDB（跨區最成熟）/ 先 TiDB（既有最熟）/ 三家同步？
4. **基準對照來源**：現行 `haproxy-3s3r` cell 作 baseline？還是另跑純 IDC 6-node？
5. **chaos C3 / C7 風險**：GCP 全 read-only / 全 cluster 寫拒 期間是否可接受？production-like 還是 lab 模式？
6. **TPCC W 分配 (Test 2)**：兩側是否 W 區分（IDC W=1-64, GCP W=65-128）？或都全 128 測極端 contention？
7. **Chaos 範圍**：7 場景全跑 / 挑 3–4 個關鍵（C1/C3/C4/C7）？
8. **Track E 與 Track D 關係**：Track E 是否取代 §5 P5 scale-out？或保留 P5 作純單區 scale-out、Track E 獨立？

### D. 文件 / Doc 落地

| # | 議題 | 我的建議 |
|---|---|---|
| **D1** | 0602.md 結構是否補 §13 數據結論 + §14 決策結果？ | **是**，Track A + Track E 跑完後補 |
| **D2** | PoC-DESIGN §6.5 是否現在寫 / 等 Track E 完？ | **A 跑完即可寫**（B–E 結論補入即可），不需等 Track E |
| **D3** | pipeline-log-template `Rebalance / Settle` 欄位現在補？ | **是**（成本極低，5 min）|

---

**下一步建議**：

1. 先答 §12 A 區（A1–A6）→ 決定單區尾巴怎麼收
2. 再答 §12 B 區（B1–B7）→ 進 Track E Pre-P0 細部規劃
3. C 區 8 個跨區開放問題建議在 B 後合併討論
4. D 區可平行進行，不擋路

**本檔狀態**：merged，0602-2.md 即將 `git rm`。
